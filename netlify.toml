# Optional alternate static deploy. Primary production flow: Framer (marketing) +
# this repo on GitHub → Vercel (API). See README.

[build]
  command = "npm ci || npm install"
  publish = "."

# Do not SPA-rewrite /* → index.html — real pages are *.html (archive, insights, etc.).
