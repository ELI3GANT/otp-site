node_modules
.env
.env.*
startup.log
.DS_Store
my.env
*.log

# Test output — keep sitemap in repo (exception below)
*.xml
!sitemap.xml

# Cursor: only share worktree setup hints (optional)
.cursor/*
!.cursor/worktrees.json

# Google Gemini / Antigravity local tool settings (regenerated per machine)
.gemini/
.playwright-cli/
output/playwright/
.vercel

# Local scratch (PDFs, exports)
tmp/

# QA artifacts (never commit)
prod-terminal-sweep.json
qa-screens/
